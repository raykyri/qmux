diff --git a/Sources/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift b/Sources/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift
index 487ee7e..3757e8c 100644
--- a/Sources/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift
+++ b/Sources/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift
@@ -18,5 +18,8 @@ final class TerminalCallbackBridge {
     weak var delegate: (any TerminalSurfaceViewDelegate)?
     /// Raw surface pointer for use in C callbacks (e.g. clipboard).
     nonisolated(unsafe) var rawSurface: ghostty_surface_t?
+    /// One-shot text supplied by a host-approved paste. Clipboard callbacks
+    /// consume this instead of reading the system pasteboard again.
+    nonisolated(unsafe) var pendingPasteText: String?
     var onCellSizeChange: ((UInt32, UInt32) -> Void)?
     var onRenderRequest: (() -> Void)?
diff --git a/Sources/GhosttyTerminal/Controller/TerminalController+Callbacks.swift b/Sources/GhosttyTerminal/Controller/TerminalController+Callbacks.swift
index b718efc..5fd5246 100644
--- a/Sources/GhosttyTerminal/Controller/TerminalController+Callbacks.swift
+++ b/Sources/GhosttyTerminal/Controller/TerminalController+Callbacks.swift
@@ -89,9 +89,6 @@ private enum TerminalCallbacks {
             .takeUnretainedValue()
         guard let surface = bridge.rawSurface else { return false }
-        #if canImport(UIKit)
-            let string = UIPasteboard.general.string
-        #elseif canImport(AppKit)
-            let string = NSPasteboard.general.string(forType: .string)
-        #endif
+        let string = bridge.pendingPasteText
+        bridge.pendingPasteText = nil
        guard let string else {
             TerminalDebugLog.log(.input, "clipboard paste read empty")
@@ -105,6 +106,11 @@ private enum TerminalCallbacks {
         )
         string.withCString { cString in
-            ghostty_surface_complete_clipboard_request(surface, cString, opaquePtr, false)
+            ghostty_surface_complete_clipboard_request(
+                surface,
+                cString,
+                opaquePtr,
+                true
+            )
         }
         TerminalDebugLog.log(.input, "clipboard paste complete")
         return true
diff --git a/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+PublicInput.swift b/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+PublicInput.swift
index 3c1ca35..3df85d4 100644
--- a/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+PublicInput.swift
+++ b/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+PublicInput.swift
@@ -13,11 +13,40 @@
         /// Send raw UTF-8 text directly to the underlying pty (bypassing
         /// key translation). Use this for synthetic input like `\x1b[Z`
         /// (Shift+Tab / CSI Z) or multi-line paste-style injections.
-        /// No-op when the surface has not been created yet.
-        public func sendText(_ text: String) {
-            surface?.sendText(text)
+        /// Returns false when the surface has not been created yet (or was
+        /// freed), so hosts can report the failure instead of silently
+        /// dropping programmatic input.
+        @discardableResult
+        public func sendText(_ text: String) -> Bool {
+            guard let surface else { return false }
+            surface.sendText(text)
+            return true
+        }
+
+        /// True once the Ghostty surface exists and can accept input.
+        public var isSurfaceLive: Bool {
+            surface != nil
         }
 
+        /// Paste text already approved by the host. The next Ghostty clipboard
+        /// request consumes this exact value and marks it confirmed, preserving
+        /// Ghostty's bracketed-paste mode without re-reading the pasteboard.
+        @discardableResult
+        public func pasteApprovedText(_ text: String) -> Bool {
+            guard let surface else { return false }
+            core.bridge.pendingPasteText = text
+            let dispatched = surface.performBindingAction("paste_from_clipboard")
+            if !dispatched {
+                core.bridge.pendingPasteText = nil
+            }
+            return dispatched
+        }
+
+        /// Return the current selection without copying through NSPasteboard.
+        public func readSelectedText() -> String? {
+            surface?.readSelection()
+        }
+
         /// Invoke a named Ghostty binding action (e.g. "copy_to_clipboard",
         /// "clear_screen"). Returns true when the action dispatched.
         @discardableResult
diff --git a/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+Input.swift b/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+Input.swift
index b4bc404..bfaeff7 100644
--- a/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+Input.swift
+++ b/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+Input.swift
@@ -109,7 +109,7 @@
             _ = copySelectedTextToPasteboard()
         }
 
-        @IBAction func paste(_: Any?) {
+        @IBAction open func paste(_: Any?) {
             if let text = NSPasteboard.general.string(forType: .string) {
                 TerminalDebugLog.log(
                     .input,
diff --git a/Sources/GhosttyTerminal/Surface/TerminalSurfaceCoordinator.swift b/Sources/GhosttyTerminal/Surface/TerminalSurfaceCoordinator.swift
index 16c35ee..54833ac 100644
--- a/Sources/GhosttyTerminal/Surface/TerminalSurfaceCoordinator.swift
+++ b/Sources/GhosttyTerminal/Surface/TerminalSurfaceCoordinator.swift
@@ -134,6 +134,7 @@ final class TerminalSurfaceCoordinator {
         let newSurface = TerminalSurface(rawSurface)
         surface = newSurface
         newSurface.setOcclusion(effectiveSurfaceVisible)
+        newSurface.setFocus(isSurfaceFocused)
         controller.shouldProcessWakeup = { [weak self] in
             self?.canRenderFrame == true
         }
diff --git a/Sources/GhosttyTerminal/Controller/TerminalController+Callbacks.swift b/Sources/GhosttyTerminal/Controller/TerminalController+Callbacks.swift
index 3147816..d7e2c66 100644
--- a/Sources/GhosttyTerminal/Controller/TerminalController+Callbacks.swift
+++ b/Sources/GhosttyTerminal/Controller/TerminalController+Callbacks.swift
@@ -39 +39 @@ private enum TerminalCallbacks {
-        terminalRunOnMain {
+        return terminalCallOnMain {
@@ -42,2 +41,0 @@ private enum TerminalCallbacks {
-
-        return false
diff --git a/Sources/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift b/Sources/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift
index d90375a..b77fba7 100644
--- a/Sources/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift
+++ b/Sources/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift
@@ -28 +28 @@ final class TerminalCallbackBridge {
-    func handleAction(_ action: ghostty_action_s) {
+    func handleAction(_ action: ghostty_action_s) -> Bool {
@@ -149,0 +150 @@ final class TerminalCallbackBridge {
+            return false
@@ -150,0 +152 @@ final class TerminalCallbackBridge {
+        return true
diff --git a/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+Input.swift b/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+Input.swift
index b4bc404..6570f95 100644
--- a/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+Input.swift
+++ b/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+Input.swift
@@ -14 +14 @@
-            inputHandler?.handleKeyDown(with: event)
+            _ = inputHandler?.handleKeyDown(with: event)
@@ -22,2 +22,8 @@
-            if keyIsBinding(event, on: surface) {
-                keyDown(with: event)
+            if let bindingFlags = keyBindingFlags(event, on: surface) {
+                let handled = inputHandler?.handleKeyDown(with: event) ?? false
+                // A performable binding is conditional on the embedding host
+                // accepting its action. Let AppKit continue when the callback
+                // reports that qmux does not implement that application action.
+                if bindingFlags.rawValue & GHOSTTY_BINDING_FLAGS_PERFORMABLE.rawValue != 0 {
+                    return handled
+                }
@@ -278 +284 @@
-        private func keyIsBinding(
+        private func keyBindingFlags(
@@ -281 +287 @@
-        ) -> Bool {
+        ) -> ghostty_binding_flags_e? {
@@ -283 +289 @@
-                return false
+                return nil
@@ -289 +295 @@
-            return text.withCString { ptr in
+            let isBinding = text.withCString { ptr in
@@ -292,0 +299 @@
+            return isBinding ? bindingFlags : nil
diff --git a/Sources/GhosttyTerminal/Platform/AppKit/TerminalKeyEventHandler@AppKit.swift b/Sources/GhosttyTerminal/Platform/AppKit/TerminalKeyEventHandler@AppKit.swift
index 4728892..cd507b4 100644
--- a/Sources/GhosttyTerminal/Platform/AppKit/TerminalKeyEventHandler@AppKit.swift
+++ b/Sources/GhosttyTerminal/Platform/AppKit/TerminalKeyEventHandler@AppKit.swift
@@ -45,2 +45,3 @@
-        func handleKeyDown(with event: NSEvent) {
-            guard let view, let surface = view.surface else { return }
+        @discardableResult
+        func handleKeyDown(with event: NSEvent) -> Bool {
+            guard let view, let surface = view.surface else { return false }
@@ -49 +50 @@
-                return
+                return true
@@ -64 +65 @@
-                return
+                return true
@@ -73,2 +74,2 @@
-                for text in collected {
-                    text.withCString { ptr in
+                return collected.reduce(false) { handled, text in
+                    let result = text.withCString { ptr in
@@ -76 +77 @@
-                        surface.sendKeyEvent(input)
+                        return surface.sendKeyEvent(input)
@@ -77,0 +79 @@
+                    return result || handled
@@ -79 +80,0 @@
-                return
@@ -85 +86 @@
-                    sendKeyEvent(
+                    return sendKeyEvent(
@@ -93 +93,0 @@
-                    return
@@ -97 +97 @@
-            sendKeyEvent(
+            return sendKeyEvent(
@@ -169 +169 @@
-        ) {
+        ) -> Bool {
@@ -179,2 +179 @@
-                surface.sendKeyEvent(input)
-                return
+                return surface.sendKeyEvent(input)
@@ -183 +182 @@
-            chars.withCString { ptr in
+            return chars.withCString { ptr in
@@ -185 +184 @@
-                surface.sendKeyEvent(input)
+                return surface.sendKeyEvent(input)
diff --git a/Sources/GhosttyTerminal/Platform/Shared/TerminalMainActor.swift b/Sources/GhosttyTerminal/Platform/Shared/TerminalMainActor.swift
index 41783dd..fe34d23 100644
--- a/Sources/GhosttyTerminal/Platform/Shared/TerminalMainActor.swift
+++ b/Sources/GhosttyTerminal/Platform/Shared/TerminalMainActor.swift
@@ -19,0 +20,20 @@ func terminalRunOnMain(
+
+/// Executes a callback on the main actor without returning to libghostty until
+/// its handled result is known. Action payload pointers are borrowed for the
+/// duration of the C callback, so this must remain synchronous.
+@inline(__always)
+func terminalCallOnMain<Result: Sendable>(
+    _ operation: @escaping @MainActor () -> Result
+) -> Result {
+    if Thread.isMainThread {
+        return MainActor.assumeIsolated {
+            operation()
+        }
+    }
+
+    return DispatchQueue.main.sync {
+        MainActor.assumeIsolated {
+            operation()
+        }
+    }
+}
diff --git a/Sources/GhosttyTerminal/Platform/Shared/TerminalHardwareKeyRouter.swift b/Sources/GhosttyTerminal/Platform/Shared/TerminalHardwareKeyRouter.swift
index 6e64724..95b21f4 100644
--- a/Sources/GhosttyTerminal/Platform/Shared/TerminalHardwareKeyRouter.swift
+++ b/Sources/GhosttyTerminal/Platform/Shared/TerminalHardwareKeyRouter.swift
@@ -105,14 +105,13 @@ enum TerminalHardwareKeyRouter {
         case 0x79:
             Data("\u{1B}[6~".utf8)
-        case 0x7B:
-            Data("\u{1B}[D".utf8)
-        case 0x7C:
-            Data("\u{1B}[C".utf8)
-        case 0x7D:
-            Data("\u{1B}[B".utf8)
-        case 0x7E:
-            Data("\u{1B}[A".utf8)
+        // Cursor-key bytes depend on DECCKM. Full-screen programs such as
+        // less enable application-cursor mode and expect SS3 (ESC O A-D),
+        // while the shell normally expects CSI (ESC [ A-D). Always let
+        // Ghostty encode arrows from its live terminal-mode state instead of
+        // bypassing it with a fixed sequence.
+        case 0x7B ... 0x7E:
+            nil
         default:
             nil
         }
     }
diff --git a/Tests/GhosttyKitTest/TerminalLifecycleTests.swift b/Tests/GhosttyKitTest/TerminalLifecycleTests.swift
index 57692fa..67c14e3 100644
--- a/Tests/GhosttyKitTest/TerminalLifecycleTests.swift
+++ b/Tests/GhosttyKitTest/TerminalLifecycleTests.swift
@@ -57,0 +58,16 @@ struct TerminalLifecycleTests {
+    @Test
+    func actionBridgeReportsOnlySupportedActionsAsHandled() {
+        let bridge = TerminalCallbackBridge()
+        var renders = 0
+        bridge.onRenderRequest = { renders += 1 }
+
+        var action = ghostty_action_s()
+        action.tag = GHOSTTY_ACTION_RENDER
+        #expect(bridge.handleAction(action))
+        #expect(renders == 1)
+
+        action.tag = GHOSTTY_ACTION_NEW_WINDOW
+        #expect(!bridge.handleAction(action))
+        #expect(renders == 1)
+    }
+
diff --git a/Sources/GhosttyTerminal/Controller/TerminalController.swift b/Sources/GhosttyTerminal/Controller/TerminalController.swift
index 8079ab0..5a76b50 100644
--- a/Sources/GhosttyTerminal/Controller/TerminalController.swift
+++ b/Sources/GhosttyTerminal/Controller/TerminalController.swift
@@ -60,0 +61 @@ public final class TerminalController {
+    var onTick: (() -> Void)?
@@ -285,0 +287 @@ public final class TerminalController {
+        onTick?()
@@ -290,0 +293,3 @@ public final class TerminalController {
+        // Even when drawing is suspended, libghostty must process queued PTY
+        // data and actions so hidden surfaces do not freeze until revealed.
+        tick()
@@ -292 +297 @@ public final class TerminalController {
-            TerminalDebugLog.log(.lifecycle, "wakeup suspended")
+            TerminalDebugLog.log(.lifecycle, "wakeup render suspended")
@@ -296 +300,0 @@ public final class TerminalController {
-        tick()
diff --git a/Tests/GhosttyKitTest/TerminalLifecycleTests.swift b/Tests/GhosttyKitTest/TerminalLifecycleTests.swift
index 67c14e3..675b2cb 100644
--- a/Tests/GhosttyKitTest/TerminalLifecycleTests.swift
+++ b/Tests/GhosttyKitTest/TerminalLifecycleTests.swift
@@ -74,14 +74,19 @@ struct TerminalLifecycleTests {
     @Test
     func `suspended wakeup does not schedule render`() {
         let controller = TerminalController()
+        var ticks = 0
         var wakeups = 0
 
+        controller.onTick = {
+            ticks += 1
+        }
         controller.shouldProcessWakeup = { false }
         controller.onWakeup = {
             wakeups += 1
         }
 
         controller.handleWakeup()
 
+        #expect(ticks == 1)
         #expect(wakeups == 0)
     }
diff --git a/Sources/GhosttyTerminal/InMemory/InMemoryTerminalSession.swift b/Sources/GhosttyTerminal/InMemory/InMemoryTerminalSession.swift
--- a/Sources/GhosttyTerminal/InMemory/InMemoryTerminalSession.swift
+++ b/Sources/GhosttyTerminal/InMemory/InMemoryTerminalSession.swift
@@ -126 +126,3 @@ public final class InMemoryTerminalSession: @unchecked Sendable {
-    public func receive(_ data: Data) {
+    /// Returns false when no live surface can accept the data yet.
+    @discardableResult
+    public func receive(_ data: Data) -> Bool {
@@ -132,4 +134,4 @@ public final class InMemoryTerminalSession: @unchecked Sendable {
                 "terminal <- host dropped \(TerminalDebugLog.describe(data))"
             )
-            return
+            return false
         }
@@ -146,3 +148,4 @@ public final class InMemoryTerminalSession: @unchecked Sendable {
             ghostty_surface_write_buffer(surface, ptr, UInt(buffer.count))
         }
+        return true
     }
diff --git a/Tests/GhosttyKitTest/TerminalLifecycleTests.swift b/Tests/GhosttyKitTest/TerminalLifecycleTests.swift
--- a/Tests/GhosttyKitTest/TerminalLifecycleTests.swift
+++ b/Tests/GhosttyKitTest/TerminalLifecycleTests.swift
@@ -1,2 +1,4 @@
 @testable import GhosttyTerminal
+import Foundation
+import GhosttyKit
 import Testing
@@ -4,3 +6,13 @@ import Testing
 @MainActor
 struct TerminalLifecycleTests {
+    @Test
+    func `in-memory receive reports a missing surface`() {
+        let session = InMemoryTerminalSession(
+            write: { _ in },
+            resize: { _ in }
+        )
+
+        #expect(!session.receive(Data("startup".utf8)))
+    }
+
     @Test
diff --git a/Tests/GhosttyKitTest/TerminalHardwareKeyRouterTests.swift b/Tests/GhosttyKitTest/TerminalHardwareKeyRouterTests.swift
index ea59c3c..042cad6 100644
--- a/Tests/GhosttyKitTest/TerminalHardwareKeyRouterTests.swift
+++ b/Tests/GhosttyKitTest/TerminalHardwareKeyRouterTests.swift
@@ -93,11 +93,17 @@ struct TerminalHardwareKeyRouterTests {
     @Test
-    func `routes app kit arrow keys directly for in memory backends`() {
+    func `routes app kit arrows through ghostty for in memory backends`() {
         let session = InMemoryTerminalSession(write: { _ in }, resize: { _ in })
         #expect(
             TerminalHardwareKeyRouter.routeAppKit(
-                keyCode: 0x7B,
+                keyCode: 0x7D,
                 backend: .inMemory(session)
-            ) == .data(Data("\u{1B}[D".utf8))
+            ) == .ghostty(GHOSTTY_KEY_ARROW_DOWN)
+        )
+        #expect(
+            TerminalHardwareKeyRouter.routeAppKit(
+                keyCode: 0x7E,
+                backend: .inMemory(session)
+            ) == .ghostty(GHOSTTY_KEY_ARROW_UP)
         )
         #expect(
             TerminalHardwareKeyRouter.routeAppKit(
diff --git a/Sources/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift b/Sources/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift
--- a/Sources/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift
+++ b/Sources/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift
@@ -52,1 +52,15 @@ final class TerminalCallbackBridge {
-        case GHOSTTY_ACTION_RING_BELL:
+        case GHOSTTY_ACTION_SCROLLBAR:
+            let scrollbar = action.action.scrollbar
+            let metrics = TerminalScrollbarMetrics(
+                total: scrollbar.total,
+                offset: scrollbar.offset,
+                length: scrollbar.len
+            )
+            TerminalDebugLog.log(
+                .actions,
+                "callback action=scrollbar total=\(metrics.total) offset=\(metrics.offset) len=\(metrics.length)"
+            )
+            (delegate as? any TerminalSurfaceScrollbarDelegate)?
+                .terminalDidChangeScrollbar(metrics)
+
+        case GHOSTTY_ACTION_RING_BELL:
diff --git a/Sources/GhosttyTerminal/Surface/TerminalSurface.swift b/Sources/GhosttyTerminal/Surface/TerminalSurface.swift
--- a/Sources/GhosttyTerminal/Surface/TerminalSurface.swift
+++ b/Sources/GhosttyTerminal/Surface/TerminalSurface.swift
@@ -202,4 +202,6 @@ public final class TerminalSurface: @unchecked Sendable {
         let text: String
         let offsetStart: UInt32
         let offsetLength: UInt32
+        let startPointX: Double
+        let baselinePointY: Double
     }
@@ -237,4 +239,6 @@ public final class TerminalSurface: @unchecked Sendable {
                 text: "",
                 offsetStart: out.offset_start,
-                offsetLength: out.offset_len
+                offsetLength: out.offset_len,
+                startPointX: Double(out.tl_px_x),
+                baselinePointY: Double(out.tl_px_y)
             )
@@ -251,4 +255,6 @@ public final class TerminalSurface: @unchecked Sendable {
             text: text,
             offsetStart: out.offset_start,
-            offsetLength: out.offset_len
+            offsetLength: out.offset_len,
+            startPointX: Double(out.tl_px_x),
+            baselinePointY: Double(out.tl_px_y)
         )
diff --git a/Sources/GhosttyTerminal/QmuxAnnotationPublic.swift b/Sources/GhosttyTerminal/QmuxAnnotationPublic.swift
new file mode 100644
--- /dev/null
+++ b/Sources/GhosttyTerminal/QmuxAnnotationPublic.swift
@@ -0,0 +1,67 @@
+import Foundation
+
+/// Ghostty's absolute scrollback geometry. `offset` is the first visible row
+/// measured from the top of retained history and `len` is the visible row count.
+public struct TerminalScrollbarMetrics: Sendable, Equatable {
+    public let total: UInt64
+    public let offset: UInt64
+    public let length: UInt64
+
+    public init(total: UInt64, offset: UInt64, length: UInt64) {
+        self.total = total
+        self.offset = offset
+        self.length = length
+    }
+}
+
+@MainActor
+public protocol TerminalSurfaceScrollbarDelegate: TerminalSurfaceViewDelegate {
+    func terminalDidChangeScrollbar(_ metrics: TerminalScrollbarMetrics)
+}
+
+#if canImport(AppKit) && !canImport(UIKit)
+    import AppKit
+
+    public struct TerminalSelectionSnapshot: Sendable, Equatable {
+        public let text: String
+        public let viewportOffsetStart: UInt32
+        public let viewportOffsetLength: UInt32
+        public let startPointX: Double
+        public let baselinePointY: Double
+
+        public init(
+            text: String,
+            viewportOffsetStart: UInt32,
+            viewportOffsetLength: UInt32,
+            startPointX: Double,
+            baselinePointY: Double
+        ) {
+            self.text = text
+            self.viewportOffsetStart = viewportOffsetStart
+            self.viewportOffsetLength = viewportOffsetLength
+            self.startPointX = startPointX
+            self.baselinePointY = baselinePointY
+        }
+    }
+
+    extension AppTerminalView {
+        /// Selection text and viewport-relative cell geometry. The offset is
+        /// trustworthy only when the embedding host observed a fully-contained
+        /// gesture; this API intentionally makes no containment claim.
+        public func readSelectionSnapshot() -> TerminalSelectionSnapshot? {
+            guard let selected = surface?.readSelectionResult() else { return nil }
+            return TerminalSelectionSnapshot(
+                text: selected.text,
+                viewportOffsetStart: selected.offsetStart,
+                viewportOffsetLength: selected.offsetLength,
+                startPointX: selected.startPointX,
+                baselinePointY: selected.baselinePointY
+            )
+        }
+
+        /// Current grid and cell dimensions in backing pixels.
+        public func terminalGridMetrics() -> TerminalGridMetrics? {
+            surface?.size()
+        }
+    }
+#endif
diff --git a/Sources/GhosttyTerminal/InMemory/InMemoryTerminalSession.swift b/Sources/GhosttyTerminal/InMemory/InMemoryTerminalSession.swift
index c209d86..8268d7a 100644
--- a/Sources/GhosttyTerminal/InMemory/InMemoryTerminalSession.swift
+++ b/Sources/GhosttyTerminal/InMemory/InMemoryTerminalSession.swift
@@ -12,6 +12,8 @@ public final class InMemoryTerminalSession: @unchecked Sendable {
     private let lock = NSLock()
     private var surface: ghostty_surface_t?
     private var lastResize: InMemoryTerminalViewport?
+    private var measuredCellWidthPixels: UInt32 = 0
+    private var measuredCellHeightPixels: UInt32 = 0
     private let writeHandler: @Sendable (Data) -> Void
     private let resizeHandler: @Sendable (InMemoryTerminalViewport) -> Void

@@ -108,16 +110,14 @@ public final class InMemoryTerminalSession: @unchecked Sendable {
         return String(decoding: bytes, as: UTF8.self)
     }

+    /// Cache cell metrics without notifying the host of a PTY resize. Surface
+    /// geometry changes before Ghostty's IO thread processes its coalesced
+    /// resize; only receiveResizeCallback may publish the new grid to a child.
     func updateViewport(_ size: TerminalGridMetrics) {
-        TerminalDebugLog.log(.metrics, "in-memory viewport update \(size.debugSummary)")
-        dispatchResize(InMemoryTerminalViewport(
-            columns: size.columns,
-            rows: size.rows,
-            widthPixels: size.widthPixels,
-            heightPixels: size.heightPixels,
-            cellWidthPixels: size.cellWidthPixels,
-            cellHeightPixels: size.cellHeightPixels
-        ))
+        lock.lock()
+        defer { lock.unlock() }
+        measuredCellWidthPixels = size.cellWidthPixels
+        measuredCellHeightPixels = size.cellHeightPixels
     }

     // MARK: - Receiving Data
@@ -240,15 +240,13 @@ public final class InMemoryTerminalSession: @unchecked Sendable {
     }

     private func mergedResize(_ resize: InMemoryTerminalViewport) -> InMemoryTerminalViewport {
-        guard let lastResize else { return resize }
-
         return InMemoryTerminalViewport(
             columns: resize.columns,
             rows: resize.rows,
-            widthPixels: resize.widthPixels == 0 ? lastResize.widthPixels : resize.widthPixels,
-            heightPixels: resize.heightPixels == 0 ? lastResize.heightPixels : resize.heightPixels,
-            cellWidthPixels: resize.cellWidthPixels == 0 ? lastResize.cellWidthPixels : resize.cellWidthPixels,
-            cellHeightPixels: resize.cellHeightPixels == 0 ? lastResize.cellHeightPixels : resize.cellHeightPixels
+            widthPixels: resize.widthPixels == 0 ? (lastResize?.widthPixels ?? 0) : resize.widthPixels,
+            heightPixels: resize.heightPixels == 0 ? (lastResize?.heightPixels ?? 0) : resize.heightPixels,
+            cellWidthPixels: resize.cellWidthPixels == 0 ? measuredCellWidthPixels : resize.cellWidthPixels,
+            cellHeightPixels: resize.cellHeightPixels == 0 ? measuredCellHeightPixels : resize.cellHeightPixels
         )
     }
 }
