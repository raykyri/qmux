[package]
name = "qmux-proto"
version = "0.3.3"
description = "Wire types for the qmux control protocol."
license = "MIT"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
